# gio-research.ai

Source for the [gio-research.ai](https://gio-research.ai/) research group
website.

## Build architecture

The site is generated by Hugo Extended 0.95.0. All templates, styles, scripts,
translations, and static assets required to render the site are committed in
the standard Hugo source directories. The build does not use Hugo modules,
does not download theme code, and does not require Go to be installed.

The rendering layer was originally derived from Wowchemy v5 at commit
`d773f436c359` and flattened into this repository for independent maintenance.
Its MIT license and copyright notice remain in `LICENSE.md`.

Do not update Hugo or modify the inherited rendering layer without testing the
complete site. The Hugo version is also pinned in `netlify.toml` so that local
and Netlify builds use the same renderer.

## Local setup on Linux or WSL2

Download and checksum-verify the pinned Hugo binary:

```bash
./scripts/bootstrap-hugo.sh
```

This installs Hugo under the ignored `.tools/` directory; it does not modify
the operating system.

Build the production site:

```bash
./scripts/build.sh
```

The generated site is written to `public/`, which is ignored by Git.

Start a local development server:

```bash
./.tools/bin/hugo server
```

The initial bootstrap requires access to the official Hugo GitHub release.
After that, builds work without network access.

## Deployment safety

Netlify builds `main` using the command and Hugo version in `netlify.toml`.
Changes should first be built locally and inspected through a deploy preview.
Do not push migration work to `main` until the preview has been approved.
